# Pokémon Dealer OS — Handover (2026-06-23)

Session goal: better comp data & RAW pricing confidence (P1 data sources → P2 comps → P3 PSA → P4 listings).

## UPDATE (end of session) — current live state
- **Pushed & deployed.** Commit `fdec242` is live in production.
- **PokeTrace: live and returning per-card data.** Burst-fix verified — e.g. Gengar Lost Origin TG RAW now returns £48.41 (US TCGPlayer NM, 6,866 sales), corroborating the £41.36 Cardmarket baseline and exposing Price Tracker's £83.50 smart price as the high outlier. The RAW cross-check now works as intended.
- **PSA cert lookup: live.** `PSA_API_TOKEN` set in Vercel; `/api/system/status` shows `psaCertLookup: true`; a real cert (79721014) returned correct parsed data incl. a dual-cert autograph.
- **eBay: blocked on eBay's developer/Production approval (~1 business day).** Account registration started; resume when approved (see §8/§13).

The sections below are the original mid-session handover; the update above supersedes any "pending push / pending deploy" notes for PokeTrace and PSA.

## 1. What is now live in production
- **PokeTrace is active in production.** `POKETRACE_API_KEY` is set in Vercel (Production+Preview, Sensitive) and a redeploy is live. `GET /api/system/status` returns `poketrace: "ready"` and `summary.secondaryCrossCheck: true`.
- The production comp pipeline works end-to-end (verified with real lookups — see §6).
- **Not yet in production:** the code committed this session (PokeTrace burst-limit fix, PSA cert lookup, eBay listing pack). These deploy automatically once you push `main` (§11).

## 2. What changed locally (committed, not pushed)
- **PokeTrace free-tier fix:** the adapter fired EU then US back-to-back; on the free tier (1 req/2s burst) the US call was rate-limited, so PokeTrace silently returned nothing. Added a configurable spacing delay (default 2100ms) before the fallback market request. Pro tier usually answers on the first (EU) call, so the delay rarely fires.
- **PokeTrace free-tier fixture + tests** locking US-only behaviour (no Cardmarket/EU, no graded on free tier).
- **PSA cert lookup (Priority 3):** new adapter, API route, and UI flow (fixture/demo mode until a token is set).
- **eBay listing pack (Priority 4, offline portion):** title/description/item-specifics/condition/UK-postage/suggested-price generator + copy-ready block + CSV export route and tests.
- **System status** now reports a PSA source row.

## 3. Files changed (commit `fdec242`, +1080 / −10)
- `src/lib/comps/sources/pokeTrace.ts` — free-tier burst spacing.
- `src/lib/comps/sources/pokeTrace.test.ts` — free-tier + spacing tests.
- `src/lib/comps/sources/__fixtures__/poketrace-cards-free-tier.json` — new fixture.
- `src/lib/psa/types.ts`, `src/lib/psa/psaCert.ts`, `src/lib/psa/psaCert.test.ts`, `src/lib/psa/__fixtures__/psa-cert.json` — PSA cert lookup.
- `src/app/api/psa/cert/route.ts` — PSA lookup endpoint.
- `src/lib/dealer/listingPack.ts`, `src/lib/dealer/listingPack.test.ts` — eBay listing pack.
- `src/app/api/export/listing-pack/route.ts` — listing-pack CSV export.
- `src/app/api/system/status/route.ts` — PSA status row.
- `src/app/page.tsx`, `src/app/globals.css` — "Verify PSA" button + result card in the Buy screen.
- `package.json` (test list), `.gitignore`.

## 4. API keys / accounts configured
- **PokeTrace** — free-tier account created; API key set in local `.env` and Vercel (value never printed/committed). Plan: **Free** (US TCGPlayer + eBay + RAW only).
- **Pokémon TCG API**, **Price Tracker**, **DATABASE_URL**, **APP_PASSWORD** — already configured (unchanged).
- **PSA** — not set up (you had no account this session). `PSA_API_TOKEN` empty → PSA runs in demo mode.
- **eBay Developer** — not set up.

## 5. Data sources now active
- **Price Tracker** — primary eBay sold comps (live).
- **Pokémon TCG API** — catalog/art + TCGPlayer/Cardmarket market baselines (live).
- **PokeTrace** — live in prod; **but on the free tier returns US raw baselines only** — no EU/Cardmarket, no graded. The burst-limit fix (committed) is needed for it to return per-card data reliably; deploy by pushing (§11).
- **Owned sales** — accumulates as you book sales.

## 6. Real comp examples tested (production)
| Card | Set | Grade | Result (headline) | Confidence behaviour | Issue |
|---|---|---|---|---|---|
| Gengar TG06/TG30 | Lost Origin Trainer Gallery | RAW | **£41.36** (Cardmarket baseline) | `sourcesDisagree:true` → picked conservative baseline over Price Tracker £83.50, would warn "check before buying" | Matched perfectly (`swsh11tg-TG06`). Correct behaviour. |
| Umbreon VMAX 215/203 | Evolving Skies | RAW | £1,297 (Price Tracker smart, conf **low**) | Catalog baseline agreed (~£1,350) → no disagreement flag | Price Tracker "ungraded" bucket noisy (min £35→max £394… 92 sales); smart-price confidence shown as low. PokeTrace empty (burst bug). |
| Charizard ex 199/165 | 151 | PSA 10 | £1,062 (Price Tracker, 250 sales, conf medium) | `single graded → manual check` (no live cross-check) | Matched (`sv3pt5-199`). Free-tier PokeTrace has no graded data, so graded comps have no cross-check until Pro/owned sales. |
| Charizard / promo / WOTC matching | Base / SVP | — | Set resolution verified offline | — | Lost Origin TG, Paldean Fates, Evolving Skies, 151, Base (4/102), SVP promo, "Moonbreon" alias all resolve to the correct set id. |

Note on absolute prices: my own price knowledge predates June 2026, so I did not "correct" headline values — the app's confidence signals (smart-price confidence, source disagreement, verdict) are the right arbiter and are behaving correctly.

## 7. PSA lookup status
- **Built and committed**, runs in **demo/fixture mode** (returns a sample Umbreon VMAX PSA 10 cert) until `PSA_API_TOKEN` is set.
- Flow: in the **Buy** screen, enter a cert number in the Cert field → **Verify PSA** → fills card name/number/grade and shows grade + population, ready to run a comp.
- Endpoint: `GET /api/psa/cert?cert=NNNNNNNN`. Adapter degrades gracefully (invalid/empty/network → `found:false` with reason).
- **To go live:** create a free PSA account at psacard.com/publicapi, generate a token (free tier = 100 lookups/day), set `PSA_API_TOKEN` in Vercel. This was not done this session because you had no PSA account ready.

## 8. Listing automation status
- **Offline pack built (committed):** eBay-ready title (≤80 chars), subtitle, description, item specifics (grader/cert/condition/set/number/rarity), eBay condition, suggested price (comp-anchored with a cost+margin floor), UK Royal Mail postage suggestion, a copy-ready text block, and a CSV export at `GET /api/export/listing-pack` (IN_STOCK/LISTED items).
- **Not built (needs eBay Developer account):** OAuth, direct draft creation via the eBay Sell/Inventory API, and UI wiring of the pack/CSV into the Listings tab. `buildListingPack()` is structured so the Sell API step can submit it directly.

## 9. Tests / checks run
- **Tests:** 246 passing. (Note: the mounted `node_modules` is macOS-native, so `npx tsx` / `npm test` is broken **in this Linux sandbox only** — `esbuild` arch mismatch. I ran the suite by compiling with `tsc` and using Node's test runner. On your Mac, `npm test` works normally.)
- **Type-check:** `tsc -p tsconfig.check.json` and full-app `tsc -p tsconfig.json --noEmit` both **0 errors** (includes `page.tsx`).
- **Build:** `next build` not runnable in-sandbox (native SWC arch mismatch); Vercel validates it on deploy. Run `npm run build` on your Mac if you want a local check first.
- **Production smoke:** `/api/system/status` + 3 real `/api/comps` lookups (§6).

## 10. Current git status
- Branch `main`, clean working tree (only the pre-existing untracked `public/icon 2.svg`).
- **1 local commit ahead of `origin/main`**, not pushed.

## 11. Commits pushed
- **None pushed yet** (sandbox has no GitHub credentials). Local commit ready: `fdec242 "Add PokeTrace free-tier handling, PSA cert lookup, eBay listing pack"`.
- **Push from your Mac** (this auto-deploys to Vercel with the PokeTrace key already set):
  ```bash
  cd "/Users/jiddle/Desktop/Pokemon Dealer OS"
  git push origin main
  ```
  After deploy, re-check a PokeTrace lookup to confirm the burst-fix works:
  `https://poke-deal.vercel.app/api/comps?name=Gengar&set=Lost%20Origin%20Trainer%20Gallery&number=TG06/TG30&grade=RAW`
  (expect the `poketrace` entry in `all[]` to have `sampleSize > 0`).

## 12. Remaining blockers
- **Push** needs your Mac (no push creds in sandbox) — §11.
- **PokeTrace per-card data** won't be reliable until the burst-fix commit is deployed (push). And free tier is **US raw only** — for EU/Cardmarket baselines and graded comps you need PokeTrace **Pro (~$19.99/mo)**.
- **PSA live** needs a PSA account + `PSA_API_TOKEN` in Vercel.
- **eBay** draft automation needs an eBay Developer account + credentials.

## 13. Recommended next task for Codex
1. **Deploy & re-verify PokeTrace** after push; confirm `all[].poketrace.sampleSize > 0` on a few RAW cards. If the free-tier US payload differs from the fixture, adjust `mapPokeTraceCardsToComp` and add coverage.
2. **RAW false-confidence guard (P2 follow-up):** when the Price Tracker "ungraded" bucket and the Cardmarket/TCGPlayer catalog baseline *agree* but are both far above the Cardmarket *low/trend* prices, treat that as low-confidence rather than "usable" (the Umbreon case showed two inflated sources agreeing → no disagreement flag). Consider weighting Cardmarket trend/avg30 more heavily for UK RAW.
3. **Wire the listing pack into the Listings tab** (button to copy the pack / download CSV) and, once an eBay Developer account exists, add the Sell API draft-create step feeding `buildListingPack()`.
4. **PSA go-live** once `PSA_API_TOKEN` is set: confirm the live `PSACert` field names against the fixture and tighten the mapper if needed.
